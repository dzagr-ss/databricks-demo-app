DATABRICKS_CONFIG_PROFILE=DEFAULT
GENIE_SPACE_ID=<your-genie-space-id>
DATABRICKS_WAREHOUSE_ID=<your-sql-warehouse-id>

# Set this to a catalog.schema that exists in your workspace.
# Run SHOW CATALOGS in Databricks SQL if you are not sure.
# Your workspace currently has a `workspace` catalog.
DEMO_SCHEMA=workspace.demo_dash_genie
